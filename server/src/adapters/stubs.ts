// ── 已迁移到独立文件 ──
// Hermes 适配器 → hermes.ts
// OpenCode 适配器 → opencode.ts
// 本文件保留作为历史占位, 不再导出任何驱动

export function registerAllDrivers(): void {
  // 驱动已在 adapters/index.ts 中通过 openclaw-cli.ts / hermes.ts / opencode.ts 注册
}
