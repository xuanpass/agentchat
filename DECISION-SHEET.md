# Agent Ops Console — 决策评审单 (Decision Review Sheet)

> 配套 `ARCHITECTURE.md` 附录 A 的 8 个决策点。
> 每个决策点: **问题 → 选项 → 取舍 → 推荐 → 决议**。
> 评审状态: ✅ **全部 8 项已于 2026-08-05 拍板**, 决议已回填至 `ARCHITECTURE.md` 附录 A。

---

## 决策 1 — 编排模式首发范围

| 项 | 内容 |
|---|---|
| **问题** | 团队"组队派活"的自动化程度, 首版做到哪一层? |
| **选项 A** | **手动编排**: UI 逐个把任务派发给成员, BFF 记录 Run/Trace, App 持有编排控制权 |
| **选项 B** | **串行状态机自动编排**: BFF 内置 orchestration state machine, 自动按序派发/等待/汇总 |
| **取舍** | A: 实现快、可控、易调试、天然带审批人; 但大规模团队时人工操作累。B: 自动化省心, 但需处理状态机回退/超时/部分失败, 且自动派发在审批不当时有失控风险。 |
| **推荐** | **A(手动优先)**。理由: 首版聚焦"接入+会话+能力同步"的可靠落地, 编排自动化牵涉大量 corner case, 应在其后迭代。 |
| **决议** | ✅ **手动编排优先**(串行状态机自动编排推迟) |

---

## 决策 2 — OpenCode 能力共享边界

| 项 | 内容 |
|---|---|
| **问题** | OpenCode 没有 Hermes/OpenClaw 那种"技能目录"(Skills/Plugins 目录)。它支持"能力"的方式是 MCP + agent/权限配置。跨系统共享能力时, OpenCode 侧能承接哪些类型? |
| **选项 A** | **只共享 MCP + agent/权限配置**; 对 OpenCode 而言, `skill`/`plugin` 类能力一律返回 `unsupported`, 不在 UI 上强制。 |
| **选项 B** | **为 OpenCode 造一个"技能桥"**: 自定义把 Hermes/OpenClaw 技能转译成 OpenCode 可用的 prompt/agent 注入(近似实现, 非原生)。 |
| **取舍** | A: 诚实、零伪造、实现简单; 但 OpenCode 能力共享面较窄。B: 覆盖面大, 但"转译出的技能"是非原生近似, 行为可能与源技能不一致, 且难验证 —— 有"宣称已实现但实际不可靠"的风险。 |
| **推荐** | **A**。符合"明确边界、不伪造能力"原则; 转译技能的可信度问题值得单独立项而不是首版硬塞。 |
| **决议** | ✅ **仅 MCP + agent/权限配置; 技能/插件类对 OpenCode 返回 unsupported** |

---

## 决策 3 — 部署形态

| 项 | 内容 |
|---|---|
| **问题** | 本应用(前端 + BFF)如何部署? 是否需要反向代理/TLS? |
| **选项 A** | **纯本地 Node 双进程**: 一个 web(Vite dev/静态) + 一个 BFF(Fastify), 同机运行 |
| **选项 B** | **Docker Compose**: 前端/BFF 容器化, 一键起停 |
| **选项 C** | 上述基础上加**反向代理 + TLS**(如 Caddy/nginx), 供公网/跨网安全暴露 |
| **取舍** | A: 最简、零基础设施, 适合本机/LAN 开发。B: 可移植、环境一致, 适合交付到服务器。C: 安全暴露远程智能体控制面所必需, 但首个里程碑不必要。 |
| **推荐** | **本机快速开发(A)**; 交付时再加 Docker Compose(B) / TLS(C)。 |
| **决议** | ✅ **本机双进程开发**; 交付再加 Compose/TLS |

---

## 决策 4 — MCP 密钥策略

| 项 | 内容 |
|---|---|
| **问题** | MCP 服务器定义常含密钥(如 `command` 里的 token、`env` 变量)。跨主机共享时如何避免泄密? |
| **选项 A** | **仅同步骨架配置**: push 不含密钥, 密钥在目标侧通过环境变量占位引用 |
| **选项 B** | **连密钥一起同步**(加密通道内) |
| **取舍** | A: 防泄密, 但目标侧需手动配置对应环境变量, 多一步人工。B: 免配置, 但把敏感密钥复制到多台远程主机, 扩展了泄露面。 |
| **推荐** | **A(骨架 + 环境变量引用)**。安全优先; UI 必须显式标注"MCP 仅同步骨架配置, 密钥请在目标主机配置环境变量"。 |
| **决议** | ✅ **仅同步骨架配置 + 目标侧环境变量引用** |

---

## 决策 5 — 凭据持久化

| 项 | 内容 |
|---|---|
| **问题** | 各远程智能体的连接凭据是否由 BFF 持久化? |
| **选项 A** | **BFF 加密落库**(AES-GCM, 密钥存环境变量), 前端不持有明文 |
| **选项 B** | **每次手动输入, 不持久化**(重启即失效) |
| **取舍** | A: 一次配置后可用, 体验好; 需管理主密钥, 加密实现有成本。B: 泄露面最小, 但每次重启都要重配, 体验差。 |
| **推荐** | **A**。主密钥由部署者管理的环境变量持有, 属可控风险; 覆盖 §7 的加密设计。 |
| **决议** | ✅ **BFF 加密落库**(用户已选) |

---

## 决策 6 — 认证模型

| 项 | 内容 |
|---|---|
| **问题** | BFF 前置认证的用户模型? |
| **选项 A** | **本地单用户**(登录一次, session token), 预留 OAuth 接口 |
| **选项 B** | **多用户 + 角色**(admin/operator/viewer) |
| **取舍** | A: 快速、够用, 契合个人/LAN 运维。B: 支持团队协作与权限隔离(如仅 lead 可改团队共享能力), 但增加用户表/角色/鉴权中间件成本。 |
| **推荐** | **A(单用户, 预留扩展)**。首版把时间花在核心功能; 多用户角色在需要团队共用控制台时再加。 |
| **决议** | ✅ **本地单用户, 预留 OAuth/多用户扩展** |

---

## 决策 7 — 模式 B 互连桥启用时机与暴露面【高风险】

| 项 | 内容 |
|---|---|
| **问题** | §5.5 模式 B(原生工具/指令桥)让"成员 A 直接调用成员 B 的工具", 本质是**在 B 机器上执行代码**(跨智能体远程执行)。何时启用、暴露到多大范围? |
| **选项 A** | **首版仅限内网/Tailscale + lead 角色可启用**, 公网绑定默认禁用; 桥调用受被调方 permission/审批策略约束(见 §7) |
| **选项 B** | **公网也可启用**, 由各被调方的 permission 矩阵兜底 |
| **取舍** | A: 把高风险能力锁在可信网络 + 最小权限, 符合"默认拒绝"安全原则。B: 便捷, 但暴露面大, 一旦某智能体 permission 配置不当即成远程代码执行风险。 |
| **推荐** | **A**。这是唯一明确"跨主机代码执行"面的决策, 必须保守。 |
| **决议** | ✅ **仅限内网/Tailscale + lead 角色可启用; 公网默认禁用** |

---

## 决策 8 — 任务(Run)与编排模式首版范围

| 项 | 内容 |
|---|---|
| **问题** | §4.5 新引入的 `Run/Task` 与编排模式, 首版做到哪层? |
| **选项 A** | **只做手动派发 + Run/Trace 记录**(每次派发记录 audit, 支持 traceId 查看调用链); 串行状态机**自动编排推迟到 M4** |
| **选项 B** | **首版即带串行状态机自动编排** |
| **取舍** | A: 先把"派发行为可追踪、可审计"做扎实, 自动编排是纯增量。B: 一步到位, 但把 Run 状态机(queued→dispatch→…→done/failed/cancelled)、超时重试和部分失败一起引入首版, 复杂度高。 |
| **推荐** | **A**。与决策 1 一致: Run 先作为"手动派发的记录与审计载体", 自动编排作为后续增强。 |
| **决议** | ✅ **手动派发 + Run/Trace 记录; 自动编排推迟 M4** |

---

## 决策汇总表

| # | 决策点 | 决议 |
|---|---|---|
| 1 | 编排模式首发 | ✅ 手动编排优先 |
| 2 | OpenCode 能力边界 | ✅ 仅 MCP + agent/权限, 技能类 unsupported |
| 3 | 部署形态 | ✅ 本机双进程开发 |
| 4 | MCP 密钥 | ✅ 仅骨架 + 环境变量引用 |
| 5 | 凭据持久化 | ✅ BFF 加密落库 |
| 6 | 认证模型 | ✅ 本地单用户, 预留扩展 |
| 7 | 模式 B 桥暴露 | ✅ 仅内网/Tailscale + lead 角色 |
| 8 | Run 首版范围 | ✅ 手动派发 + Trace, 自动编排推迟 M4 |
